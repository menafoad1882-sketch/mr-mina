# 🚀 دليل تشغيل الموقع (محليًا + أونلاين)

الكود اتظبط عشان يشتغل في الحالتين. اتبع القسم اللي يناسبك.

---

## 💻 أولًا: التشغيل على جهازك (محليًا)

```bash
cd teacher_app
pip install -r requirements.txt
python app.py
```
افتح المتصفح على: **http://127.0.0.1:5000**

- اسم المستخدم: `admin` — كلمة المرور: `admin123`
- ده الوضع المناسب للتجربة والتطوير.

---

## 🌐 ثانيًا: رفع الموقع أونلاين (مجانًا على Render)

**Render** أسهل استضافة مجانية لمبتدئ، وبتقرأ الإعدادات تلقائيًا من ملف `render.yaml` الموجود عندك.

> 🐳 **مهم (OCR للملفات الممسوحة):** `render.yaml` مضبوط على **Docker** (`runtime: docker`)
> ليتم تثبيت **Tesseract OCR + اللغة العربية** تلقائيًا عبر `Dockerfile` عند النشر.
> بهذا تعمل قراءة ملفات PDF الممسوحة صوريًا بالعربية في بيئة الإنتاج بدون أي تثبيت يدوي.

### الخطوات بالترتيب:

### 1) ارفع الكود على GitHub (لو لسه)
راجع ملف `GITHUB.md`.

### 2) اعمل حساب على Render
- ادخل على **https://render.com** واعمل Sign Up (يفضّل بحساب GitHub عشان يربطهم تلقائيًا).

### 3) أنشئ الخدمة
- من لوحة Render اضغط **New +** ثم **Blueprint**.
- اختار مستودع (repo) المشروع بتاعك من GitHub.
- Render هيلاقي ملف `render.yaml` ويقرأه لوحده — اضغط **Apply**.

### 4) ⚠️ ضيف رابط قاعدة البيانات (خطوة مهمة جدًا)
الخطة المجانية في Render **لا تدعم القرص الدائم**، فالبيانات بتتخزّن في **Supabase**:
- من لوحة Render > خدمتك > **Environment** > أضف متغيرًا:
  - **Key:** `DATABASE_URL`
  - **Value:** رابط Supabase (استخدم **Connection Pooling / Transaction mode، المنفذ 6543**)
  - وتأكد إنك استبدلت `[YOUR-PASSWORD]` بكلمة مرور قاعدة البيانات الحقيقية.
- احفظ — Render هيعيد النشر تلقائيًا.

> 💡 لو لسه ما عملتش Supabase: راجع خطوات إنشائه في ملف `VERCEL.md` (نفس الطريقة). مجاني وبياخد ٥ دقايق.

### 5) افتح موقعك
- هيديك رابط شكله: `https://teacher-app-xxxx.onrender.com`
- افتحه، سجّل دخول بـ `admin` / `admin123`، وغيّر كلمة المرور فورًا من الإعدادات.
- من الإعدادات > تبويب Supabase هتلاقي مكتوب: "✅ متصل بـ Supabase / Postgres".

> ✅ الجداول بتتعمل تلقائيًا أول تشغيل — مش محتاج تكتب SQL يدوي. و`SECRET_KEY` بيتولّد آمن تلقائيًا.

> 💾 **بديل (خطة مدفوعة):** لو مشترك في خطة Starter أو أعلى وعايز SQLite على قرص دائم
> بدل Supabase — راجع التعليقات في آخر ملف `render.yaml` (فيه كتلة `disk:` جاهزة تفعّلها).

---

## 🔁 بدائل استضافة تانية

### Railway.app
- New Project > Deploy from GitHub repo.
- بيقرأ `Procfile` تلقائيًا.
- ضيف Variable: `FLASK_ENV=production`، و`DATA_DIR=/data` لو ضفت Volume.

### PythonAnywhere (مناسب لمصر، مجاني)

⚠️ **مهم — PythonAnywhere مختلف عن Render:**
- **لا يستخدم gunicorn ولا `render.yaml` ولا `Procfile`** — بل ملف **WSGI**.
- **الخطة المجانية تحظر الإنترنت الخارجي** (قائمة بيضاء لا تشمل Supabase) →
  استخدم **SQLite** (نظام الملفات دائم فالبيانات لا تضيع)، **ولا تضبط `DATABASE_URL`**.
- ملف WSGI جاهز في المشروع: **`pythonanywhere_wsgi.py`** (انسخ منه).

**الخطوات:**
1. اعمل حساب على **https://www.pythonanywhere.com** (Beginner مجاني).
2. من تبويب **Consoles** > افتح **Bash console** وشغّل (استبدل الرابط برابط ريبو GitHub بتاعك):
   ```bash
   git clone https://github.com/USERNAME/REPO.git teacher_app
   cd teacher_app
   pip3.10 install --user -r requirements.txt
   ```
   > 💡 لو `PyMuPDF`/`Pillow` أخدت وقت أو فشلت على الخطة المجانية، ممكن تتخطاها —
   > الموقع بيشتغل من غيرها (بيعطّل ميزة OCR للـ PDF فقط).
3. من تبويب **Web** > **Add a new web app** > اختر **Manual configuration** > **Python 3.10**.
4. في قسم **Code** اضبط:
   - **Source code:** `/home/USERNAME/teacher_app`
   - **Working directory:** `/home/USERNAME/teacher_app`
5. اضغط على رابط ملف **WSGI configuration file**، امسح محتواه، والصق محتوى ملف
   **`pythonanywhere_wsgi.py`** من مشروعك، وعدّل فيه:
   - `PROJECT_HOME = "/home/USERNAME/teacher_app"` (استبدل `USERNAME` باسم حسابك)
   - `SECRET_KEY` لأي نص عشوائي طويل.
   احفظ.
6. ارجع لتبويب **Web** واضغط **Reload**. افتح رابطك: `https://USERNAME.pythonanywhere.com`
7. سجّل دخول `admin` / `admin123` وغيّر كلمة المرور فورًا.

> ✅ نظام ملفات PythonAnywhere دائم، فقاعدة SQLite وبيانات الطلاب لا تضيع.
> ⚠️ على الخطة المجانية قد لا يعمل إرسال واتساب/الإيميل/الذكاء الاصطناعي الخارجي
> بسبب حظر الإنترنت — للوصول الكامل تحتاج خطة مدفوعة (وقتها يمكن تفعيل Supabase أيضًا).

### أي استضافة بتدعم Python
المطلوب:
- أمر التشغيل: `gunicorn app:app --bind 0.0.0.0:$PORT`
- متغيرات البيئة: `FLASK_ENV=production` و `SECRET_KEY=<أي نص عشوائي طويل>`
- (اختياري) `DATA_DIR=<مسار مجلد دائم>` لحفظ قاعدة البيانات.

---

## ⚙️ متغيرات البيئة المدعومة

| المتغير | الوصف | مثال |
|---------|-------|------|
| `PORT` | المنفذ (الاستضافة بتحدده تلقائيًا) | `10000` |
| `SECRET_KEY` | مفتاح تأمين الجلسات — **مهم جدًا للأمان** | نص عشوائي طويل |
| `FLASK_ENV` | `production` لإطفاء وضع التطوير على الاستضافة | `production` |
| `DATA_DIR` | مجلد حفظ قاعدة البيانات (قرص دائم) | `/var/data` |

---

## 🩺 حل المشاكل الشائعة

| المشكلة | السبب / الحل |
|---------|--------------|
| **Application failed / crashed** | تأكد إن `requirements.txt` فيه `gunicorn` (موجود). وإن أمر التشغيل `gunicorn app:app`. |
| **صفحة بيضا / 502** | أول تشغيل بياخد دقيقة. حدّث الصفحة بعد شوية. راجع Logs في لوحة الاستضافة. |
| **البيانات بتتمسح** | على الخطة المجانية لازم تضبط `DATABASE_URL` (Supabase). من غيره البيانات مؤقتة وتتمسح مع كل إعادة تشغيل. |
| **Deploy failed / disk not supported on free** | الخطة المجانية مبتدعمش القرص الدائم. الملف الحالي متظبّط على Supabase. لو عايز قرص دائم لازم خطة مدفوعة. |
| **الكاميرا (QR) مش بتفتح** | الكاميرا محتاجة HTTPS. روابط Render/Railway بتكون HTTPS تلقائيًا ✅ |
| **الموقع "نام" وبطيء أول مرة** | الخطة المجانية بتنام بعد خمول؛ أول زيارة بتوقظه (١٥-٣٠ ثانية). طبيعي. |

---

## 🔒 قبل الاستخدام الحقيقي
1. غيّر كلمة مرور `admin` من الإعدادات.
2. تأكد إن `SECRET_KEY` مضبوط كمتغير بيئة (Render بيعمله تلقائيًا).
3. لو عايز أمان أكتر للبيانات، فعّل **Supabase** من الإعدادات كنسخة احتياطية سحابية.

---

## 🤖 مولّد الامتحانات بالذكاء الاصطناعي (اختياري)

### اختيار المزوّد
من **الإعدادات > 🤖 الذكاء الاصطناعي** اختر المزوّد وأدخل المفتاح:
- **ChatGPT (OpenAI):** المفتاح من platform.openai.com
- **Google Gemini:** المفتاح من aistudio.google.com
- **مخصّص:** أي خدمة متوافقة مع OpenAI (أدخل Base URL + الموديل)

بدون مفتاح، يعمل مولّد محلي مبسّط.

### دعم قراءة الملفات الممسوحة (OCR) — بدون أي برامج نظام ✅
النظام يقرأ ملفات PDF تلقائيًا:
- **PDF نصي:** يُستخرج النص مباشرة عبر PyMuPDF (لا يحتاج أي إعداد).
- **PDF ممسوح صوريًا (صور):** تُحوَّل الصفحات لصور عبر PyMuPDF (بدون poppler)،
  ثم يقرأها **نموذج رؤية الذكاء الاصطناعي** (ChatGPT / Gemini) ويستخرج النص العربي بدقة.

**المتطلبات (كلها حزم pip تُثبَّت تلقائيًا من requirements.txt، بدون أي برامج نظام):**
`PyMuPDF`، `Pillow`، `openai`. لذلك يعمل الـ OCR على أي استضافة (Render / Railway /
Vercel / PythonAnywhere ...) بدون تثبيت tesseract أو poppler.

**الشرط الوحيد لقراءة الملفات الممسوحة:** ربط مزوّد ذكاء اصطناعي واحد
(من الإعدادات > الذكاء الاصطناعي) لأن الـ OCR يتم عبر نموذج الرؤية.

> `pytesseract` مذكور في requirements كخيار محلي إضافي فقط — غير مطلوب على الاستضافة.

---

## 🗄️ ملف مخطط قاعدة البيانات (schema.sql)
- يُحدَّث تلقائيًا عند كل تشغيل ليطابق أحدث بنية للتطبيق.
- ملفان: `schema.sql` (SQLite) و `schema_postgres.sql` (Postgres/Supabase).
- يمكن تحميلهما من **الإعدادات > Supabase**، ويُستخدمان لإعادة إنشاء القاعدة.

---

## 💬 معمارية واتساب (UltraMsg) — مهم

- الإرسال يتم **من الخادم (backend)** حصريًا: المتصفح يستدعي `/whatsapp/send_api`،
  والخادم يقرأ التوكن من قاعدة البيانات ويرسل إلى UltraMsg. **التوكن لا يظهر في المتصفح إطلاقًا.**
- الطلب يُرسل بترويسة **User-Agent متصفح حقيقي** لتفادي حظر Cloudflare
  (الخطأ 1010 / browser_signature_banned). هذا الخطأ **ليس** بسبب التوكن أو رقم الـ Instance.
- **لا يُعاد** إرسال طلب حصل على Cloudflare 1010 (غير قابل لإعادة المحاولة).
- سجل الرسائل (الإعدادات → واتساب → سجل الرسائل) يعرض: النجاح/الفشل، كود HTTP،
  تصنيف الخطأ (بيانات اعتماد/instance/cloudflare/اتصال/rate limit/خادم)، النوع، الوقت، المستلم.

## 🤖 مفاتيح الذكاء الاصطناعي (مستقلة)

- يُحفظ مفتاح **ChatGPT** ومفتاح **Gemini** بشكل مستقل في قاعدة البيانات
  (`ai_key_openai` / `ai_key_gemini`)، وحفظ أحدهما لا يمسح الآخر.
- المزوّد المختار (`ai_provider`) هو الذي يُستخدم فعليًا في توليد الامتحان وقراءة OCR،
  ويستمر بعد إعادة التشغيل/تسجيل الدخول.

## 📄 OCR لملفات PDF الممسوحة

- استخراج النص المباشر عبر **PyMuPDF** (حزمة pip في requirements — لا برامج نظام).
- الملفات الممسوحة (صور): تُحوّل صفحاتها لصور بـ PyMuPDF ثم تُقرأ عبر **نموذج رؤية**
  المزوّد المختار (ChatGPT/Gemini) لاستخراج النص العربي — دون tesseract/poppler.
