# ▲ رفع الموقع على Vercel + Supabase

الموقع اتظبط عشان يشتغل على Vercel، وبيانات الطلاب تتخزّن في **Supabase** (قاعدة بيانات دائمة) فلا تضيع.

> ⚠️ **مهم:** Vercel لوحده مبيحفظش البيانات (serverless). لازم Supabase عشان البيانات تفضل. الموقع بيعمل الجداول بنفسه أول تشغيل — مش محتاج تكتب SQL يدوي.

---

## 1️⃣ جهّز Supabase (٥ دقائق)

1. ادخل **https://supabase.com** > اعمل حساب > **New Project**.
2. اختر اسمًا وكلمة مرور قوية لقاعدة البيانات (**احفظها**).
3. استنى لحد ما المشروع يجهز (١-٢ دقيقة).
4. خذ رابط الاتصال:
   - من **Project Settings** (الترس) > **Database** > **Connection string** > اختر تبويب **URI**.
   - هيكون شكله:
     ```
     postgresql://postgres:[YOUR-PASSWORD]@db.xxxxxxxx.supabase.co:5432/postgres
     ```
   - **مهم:** استبدل `[YOUR-PASSWORD]` بكلمة مرور قاعدة البيانات الحقيقية.

   > 💡 **نصيحة (لو واجهت مشاكل اتصال على Vercel):** استخدم رابط **Connection Pooling** بدلًا من المباشر
   > (نفس الصفحة > قسم *Connection pooling* > **Transaction** mode، المنفذ غالبًا `6543`).
   > هذا الأنسب للبيئات serverless زي Vercel.

---

## 2️⃣ ارفع الكود على GitHub
لو لسه، راجع `GITHUB.md` واعمل `git push`.

---

## 3️⃣ انشر على Vercel

1. ادخل **https://vercel.com** > سجّل دخول بحساب GitHub.
2. **Add New...** > **Project** > اختر مستودع المشروع بتاعك > **Import**.
3. Vercel هيتعرّف على إعدادات المشروع من ملف `vercel.json` تلقائيًا.
4. قبل ما تضغط Deploy، افتح **Environment Variables** وضيف المتغيرات دي:

   | Name | Value |
   |------|-------|
   | `DATABASE_URL` | رابط Supabase URI (اللي أخذته فوق) |
   | `SECRET_KEY` | أي نص عشوائي طويل (مثلًا ٣٢ حرف) |
   | `FLASK_ENV` | `production` |

5. اضغط **Deploy** واستنى دقيقة-دقيقتين.
6. هيديك رابط زي `https://your-project.vercel.app` — افتحه!

---

## 4️⃣ أول تسجيل دخول
- اسم المستخدم: `admin` — كلمة المرور: `admin123`
- **غيّرهما فورًا** من الإعدادات > الحساب.
- من الإعدادات > تبويب Supabase هتلاقي مكتوب: "✅ متصل بـ Supabase / Postgres".

---

## 🔁 التحديثات المستقبلية
أي `git push` على GitHub، Vercel بيعيد النشر تلقائيًا.

---

## 🩺 حل المشاكل

| المشكلة | الحل |
|---------|------|
| **500 / Internal Server Error** | تأكد إن `DATABASE_URL` مضبوط صح وكلمة المرور صحيحة (مش `[YOUR-PASSWORD]`). راجع Logs في Vercel. |
| **مشاكل اتصال / timeout** | استخدم رابط **Connection Pooling** (Transaction mode, port 6543) بدل المباشر. |
| **الكاميرا (QR) مش بتفتح** | Vercel بيديك HTTPS تلقائيًا فالكاميرا المفروض تشتغل ✅ |
| **البيانات مش بتتحفظ** | تأكد إن `DATABASE_URL` مضبوط. من غيره الموقع بيستخدم SQLite مؤقت وبيتمسح. |
| **تسجيل الدخول بيخرجني** | تأكد إن `SECRET_KEY` مضبوط كمتغير بيئة (ثابت). |

---

## ملاحظات
- الجداول تُنشأ تلقائيًا أول ما الموقع يشتغل — مش محتاج SQL يدوي.
- تقدر تشوف بياناتك مباشرة من Supabase > Table Editor.
- محليًا (على جهازك) الموقع لسه بيشتغل بـ SQLite عادي من غير أي إعداد.
