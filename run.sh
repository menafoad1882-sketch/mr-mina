#!/usr/bin/env bash
# تشغيل نظام إدارة المدرس
cd "$(dirname "$0")"
echo "======================================"
echo "   نظام إدارة المدرس - جاري التشغيل"
echo "======================================"
# تثبيت المكتبات المطلوبة لو مش موجودة
pip3 install flask openpyxl >/dev/null 2>&1
# تهيئة قاعدة البيانات لو مش موجودة
[ -f teacher.db ] || python3 database.py
echo ""
echo "افتح المتصفح على العنوان التالي:"
echo "   http://127.0.0.1:5000"
echo ""
echo "(للإيقاف اضغط Ctrl+C)"
echo "======================================"
python3 app.py
